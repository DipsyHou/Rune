openai>=1.0.0
pyyaml>=6.0
rich>=13.0.0
prompt_toolkit>=3.0.0
python-dotenv>=1.0.0

# --- Plugin Dependencies ---
# UI Automation (used by wechat plugin on Windows)
uiautomation>=2.0.0; sys_platform == 'win32'
pyperclip>=1.8.2; sys_platform == 'win32'

# Web Automation (used by browser plugin)
playwright>=1.40.0